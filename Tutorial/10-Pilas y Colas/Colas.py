# Colas

cola = [1,2,3]

# Agregando elementos por el final de la cola
cola.append(4)
cola.append(5)

print(cola)

# Sacando elementos por el principio de una cola
n = cola.pop(0)
print(f"Sacando el elelemento {n}")

n = cola.pop(0)
print(f"Sacando el elelemento {n}")

print(cola)